<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessApp.Views.Profil"
             xmlns:vm="clr-namespace:FitnessApp.ViewModels"
             Title="{Binding Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" BackgroundColor="Red" VerticalOptions="Start">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="xamarin_logo.png" HeightRequest="50" WidthRequest="50" VerticalOptions="Start"/>
            <Label Grid.Column="1" Text="Vorname Nachname" FontSize="Large" TextColor="Black" VerticalOptions="Center"/>
        </Grid>
    </Grid>
</ContentPage>
