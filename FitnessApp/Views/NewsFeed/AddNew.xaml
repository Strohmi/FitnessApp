<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessApp.AddNew"
             Title="{Binding Title}">

    <Grid BackgroundColor="Black">
        <ListView x:Name="listOptions" HasUnevenRows="True" ItemsSource="{Binding Options}" BackgroundColor="Transparent" SeparatorVisibility="None" ItemSelected="Selection" Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Value}" FontSize="Large" TextColor="Black" Padding="0,20" Margin="15,10,5,10" BackgroundColor="Orange" HorizontalTextAlignment="Center"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>
