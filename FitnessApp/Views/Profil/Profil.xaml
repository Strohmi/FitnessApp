<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitnessApp.Profil"
             Title="{Binding Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" BackgroundColor="Red" VerticalOptions="Start">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="Plus.png" HeightRequest="50" WidthRequest="50"/>
            <Label Grid.Column="1" FontSize="Large" TextColor="Black" VerticalOptions="Center">
                <Label.Text>
                    <MultiBinding StringFormat="{}{0} {1}">
                        <Binding Path="User.Vorname"/>
                        <Binding Path="User.Nachname"/>
                    </MultiBinding>
                </Label.Text>
            </Label>
        </Grid>

        <Grid Grid.Row="1" x:Name="gridEdit" IsVisible="false" BackgroundColor="Yellow" VerticalOptions="Start">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>


            <Entry Grid.Row="0" x:Name="vorname" Placeholder="Vorname" PlaceholderColor="Gray" Text="{Binding User.Vorname}" TextColor="Black" BackgroundColor="White"/>
            <Entry Grid.Row="1" x:Name="nachname" Placeholder="Nachname" PlaceholderColor="Gray" Text="{Binding User.Nachname}" TextColor="Black" BackgroundColor="White"/>
            <DatePicker Grid.Row="2" Date="{Binding User.Geburtsdatum}" TextColor="Black" BackgroundColor="White" MaximumDate="{Binding MaxDate}" MinimumDate="{Binding MinDate}"/>
        </Grid>
    </Grid>
</ContentPage>
